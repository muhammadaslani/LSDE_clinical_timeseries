model:
  type: "LatentSDE"
  context_dim: 32

  SDE:
    drift:
      type: "MLP"
      hidden_size: 32
      depth: 1
      activation: swish
    drift_aug:
      type: "MLP"
      hidden_size: 32
      depth: 1
      activation: swish
    diffusion:
      type: "SparseMLP"
      activation: sigmoid
    solver: EM()
    kwargs:
      dt: 0.01

  validation:
    solver: EM()
    kwargs:
      dt: 0.01

experiment:
  name: "LatentSDE_Glucose_V0"
  description: "Latent SDE model for Bergman glucose forecasting"
  path: "experiments/"
